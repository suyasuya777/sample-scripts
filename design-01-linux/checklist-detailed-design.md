# Linux サーバー構築 詳細設計チェックリスト

> 基本設計チェックリストが「要件を満たす実現方式・構成方針が決定されているか」を確認するのに対し、  
> 本チェックリストは **「実装可能なレベルまでパラメーター・設定値が確定しているか」** を確認します。

---

## 1. 目的・スコープ（詳細）

### 1.1 環境構成一覧

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 1.1.1 | 開発/検証/本番の各環境のサーバー台数・スペックを確定したこと | 必須 | [ ] | |
| 1.1.2 | 環境ごとのアクセス制御方針（踏み台 / VPN / Ansible接続元等）を明記したこと | 必須 | [ ] | |

### 1.2 命名規則

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 1.2.1 | 全サーバー種別の命名パターン（`<役割>-<環境>-<連番>.example.com`等）を定義したこと | 必須 | [ ] | |
| 1.2.2 | `{env}` `{role}` 等のプレースホルダーを実際の値に置換可能な形にしたこと | 必須 | [ ] | |
| 1.2.3 | Ansibleインベントリ・ロール名の命名規則を定義したこと | 推奨 | [ ] | |

### 1.3 パラメーター管理規則

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 1.3.1 | ★マークの未確定パラメーターを一覧化し、確定期限を設定したこと | 必須 | [ ] | |
| 1.3.2 | 環境ごとの差分パラメーターをAnsible変数（group_vars / host_vars）で管理する方針を確定したこと | 推奨 | [ ] | |
| 1.3.3 | 機密情報（パスワード等）をAnsible Vault等で暗号化管理する方針を確定したこと | 推奨 | [ ] | |

---

## 2. システム概要（詳細）

### 2.1 サーバー構成・通信表

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 2.1.1 | サーバー構成図を作成・添付したこと | 必須 | [ ] | |
| 2.1.2 | コンポーネント間通信の送信元/宛先/プロトコル/ポートを一覧化したこと | 必須 | [ ] | |
| 2.1.3 | 通信表とfirewalld設定（14章）の整合性を確認したこと | 必須 | [ ] | |

### 2.2 非機能要件（数値詳細）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 2.2.1 | 各非機能要件項目に「測定方法」「測定期間」を明記したこと | 必須 | [ ] | |
| 2.2.2 | レスポンスタイムのp50/p95/p99を個別に定義したこと | 推奨 | [ ] | |

### 2.3 環境別スペック差分サマリ

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 2.3.1 | 開発/検証/本番の差分項目（インスタンスタイプ・メモリ・ディスク等）を一覧化したこと | 必須 | [ ] | |
| 2.3.2 | 差分項目が後続章（3〜30）の各設定表と矛盾していないか確認したこと | 必須 | [ ] | |

---

## 3. OS基本設定パラメーター

### 3.1 システム基本情報

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 3.1.1 | ホスト名（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 3.1.2 | タイムゾーン（Asia/Tokyo）・ロケール（LANG=ja_JP.UTF-8）を設定したこと | 必須 | [ ] | |
| 3.1.3 | OSバージョン（AlmaLinux 9.4）・カーネルバージョン（5.14.0-427.xx.x）を実機確認・記入したこと | 必須 | [ ] | |
| 3.1.4 | systemdバージョン（252）を確認したこと | 必須 | [ ] | |

### 3.2 GRUB2 パラメーター（`/etc/default/grub`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 3.2.1 | `GRUB_TIMEOUT=5` を設定したこと | 必須 | [ ] | |
| 3.2.2 | `GRUB_CMDLINE_LINUX` に `audit=1 selinux=1 security=selinux` を含めたこと | 必須 | [ ] | |
| 3.2.3 | `GRUB_ENABLE_BLSCFG=true` を設定したこと（RHEL9デフォルト） | 必須 | [ ] | |
| 3.2.4 | 設定反映コマンド（grub2-mkconfig）をUEFI/BIOS別に記載したこと | 必須 | [ ] | |

### 3.3 systemd 設定パラメーター

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 3.3.1 | デフォルトターゲット（multi-user.target）を設定したこと | 必須 | [ ] | |
| 3.3.2 | journald永続化（`Storage=persistent`）・最大サイズ（`SystemMaxUse=1G`）を設定したこと | 必須 | [ ] | |
| 3.3.3 | journald `RateLimitBurst=10000` を設定したこと | 推奨 | [ ] | |

---

## 4. カーネルパラメーター（`/etc/sysctl.d/99-custom.conf`）

### 4.1 ネットワークセキュリティ

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 4.1.1 | `net.ipv4.ip_forward` の本番設定値（★）を確定したこと | 必須 | [ ] | |
| 4.1.2 | `send_redirects` / `accept_redirects` を全て 0 に設定したこと | 必須 | [ ] | |
| 4.1.3 | `tcp_syncookies=1`（SYN Flood対策）を設定したこと | 必須 | [ ] | |
| 4.1.4 | `rp_filter=1`（スプーフィング対策）を設定したこと | 必須 | [ ] | |
| 4.1.5 | `icmp_echo_ignore_broadcasts=1`（Smurf対策）を設定したこと | 必須 | [ ] | |
| 4.1.6 | `log_martians=1`（不正パケットログ）を設定したこと | 必須 | [ ] | |
| 4.1.7 | `tcp_timestamps=0`（情報漏洩防止）を設定したこと | 必須 | [ ] | |
| 4.1.8 | IPv6 disable の本番設定値（★）を確定したこと | 必須 | [ ] | |

### 4.2 カーネルセキュリティ・パフォーマンス

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 4.2.1 | `kernel.sysrq=0` / `kernel.randomize_va_space=2` / `kernel.dmesg_restrict=1` を設定したこと | 必須 | [ ] | |
| 4.2.2 | `fs.suid_dumpable=0` / `fs.protected_hardlinks=1` / `fs.protected_symlinks=1` を設定したこと | 必須 | [ ] | |
| 4.2.3 | `vm.swappiness` の本番設定値（★：DBは10・一般は60等）を確定したこと | 必須 | [ ] | |
| 4.2.4 | `net.core.somaxconn` / `tcp_max_syn_backlog` の本番設定値（★）を確定したこと | 推奨 | [ ] | |

---

## 5. ストレージ・ファイルシステムパラメーター

### 5.1 パーティション構成（`/etc/fstab`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 5.1.1 | 各マウントポイント（/ / /boot / /var / /home / swap）のサイズを確定したこと | 必須 | [ ] | |
| 5.1.2 | `/etc/fstab` をUUIDベースで記述したこと | 必須 | [ ] | |
| 5.1.3 | /var に `nodev,nosuid` マウントオプションを設定したこと | 必須 | [ ] | |
| 5.1.4 | /home に `nodev,nosuid,noexec` マウントオプションを設定したこと | 必須 | [ ] | |
| 5.1.5 | /tmp に `nodev,nosuid,noexec` マウントオプションを設定したこと | 必須 | [ ] | |

---

## 6. ネットワーク設定パラメーター

### 6.1 NIC設定（NetworkManager keyfile形式）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 6.1.1 | keyfile形式（`/etc/NetworkManager/system-connections/`）で設定したこと（ifcfg形式は廃止方向） | 必須 | [ ] | |
| 6.1.2 | 固定IPアドレス（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 6.1.3 | ゲートウェイ（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 6.1.4 | DNSサーバー（★設定値）を確定・記入したこと | 必須 | [ ] | |

### 6.2 名前解決設定

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 6.2.1 | `/etc/hosts` に自ホスト・主要サーバーのエントリを設定したこと | 必須 | [ ] | |
| 6.2.2 | `/etc/resolv.conf` の `nameserver`（★設定値）・`search`/`domain` を設定したこと | 必須 | [ ] | |
| 6.2.3 | `/etc/nsswitch.conf` の `hosts` 行を `files dns` 順に設定したこと | 必須 | [ ] | |

---

## 7. SSH設定パラメーター（`/etc/ssh/sshd_config`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 7.1 | `Port`（★設定値：デフォルト22から変更推奨）を確定・記入したこと | 必須 | [ ] | |
| 7.2 | `PermitRootLogin no` を設定したこと | 必須 | [ ] | |
| 7.3 | `MaxAuthTries 3` を設定したこと | 必須 | [ ] | |
| 7.4 | `PubkeyAuthentication yes` / `PasswordAuthentication no` を設定したこと | 必須 | [ ] | |
| 7.5 | `PermitEmptyPasswords no` / `KbdInteractiveAuthentication no` を設定したこと（新パラメーター名） | 必須 | [ ] | |
| 7.6 | `X11Forwarding no` を設定したこと | 必須 | [ ] | |
| 7.7 | `ClientAliveInterval 300` / `ClientAliveCountMax 3` を設定したこと | 必須 | [ ] | |
| 7.8 | HostKeyにEd25519（`ssh_host_ed25519_key`）を最優先で設定したこと | 必須 | [ ] | |
| 7.9 | `Ciphers` に chacha20-poly1305 / aes256-gcm 等の安全な暗号スイートのみ設定したこと | 必須 | [ ] | |
| 7.10 | `KexAlgorithms` に curve25519-sha256 を最優先で設定したこと | 必須 | [ ] | |
| 7.11 | `AllowUsers` または `AllowGroups`（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 7.12 | `LogLevel VERBOSE` を設定したこと | 推奨 | [ ] | |

---

## 8. ユーザー・PAM設定パラメーター

### 8.1 パスワードポリシー（`/etc/security/pwquality.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 8.1.1 | `minlen`（最小文字数：推奨12以上）を設定したこと | 必須 | [ ] | |
| 8.1.2 | `minclass`（最小文字種数：推奨3以上）を設定したこと | 必須 | [ ] | |
| 8.1.3 | `maxrepeat`（連続同一文字の上限）を設定したこと | 必須 | [ ] | |
| 8.1.4 | `dcredit` / `ucredit` / `lcredit` / `ocredit` を設定したこと | 必須 | [ ] | |

### 8.2 アカウントロック（`/etc/security/faillock.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 8.2.1 | `deny`（ロックまでの失敗回数：推奨5）を設定したこと | 必須 | [ ] | |
| 8.2.2 | `fail_interval`（失敗カウント期間）・`unlock_time`（ロック解除待機秒数）を設定したこと | 必須 | [ ] | |
| 8.2.3 | `/etc/pam.d/system-auth` と `/etc/pam.d/password-auth` に `pam_faillock` を設定したこと | 必須 | [ ] | |

### 8.3 パスワード有効期限（`/etc/login.defs`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 8.3.1 | `PASS_MAX_DAYS`（最大有効日数）・`PASS_MIN_DAYS`（最小変更間隔）を設定したこと | 必須 | [ ] | |
| 8.3.2 | `PASS_WARN_AGE`（期限切れ警告日数）を設定したこと | 必須 | [ ] | |

---

## 9. DNS設定パラメーター

### 9.1 DNSコンテンツサーバー（BIND 9.16 / named）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 9.1.1 | `listen-on port 53` の本番IPアドレス（★）を確定・記入したこと | 必須 | [ ] | |
| 9.1.2 | `listen-on-v6 { none; }` を設定したこと | 必須 | [ ] | |
| 9.1.3 | `allow-query` の許可元（★内部NW）を確定・記入したこと | 必須 | [ ] | |
| 9.1.4 | `recursion no`（権威DNSのため再帰禁止）を設定したこと | 必須 | [ ] | |
| 9.1.5 | `allow-recursion` 行を削除または無効化したこと（`recursion no` のため不要） | 必須 | [ ] | |
| 9.1.6 | `dnssec-validation auto` を設定したこと | 必須 | [ ] | |
| 9.1.7 | `version "none"`（バージョン情報非表示）を設定したこと | 必須 | [ ] | |
| 9.1.8 | `allow-transfer` のセカンダリDNS-IP（★）を確定・記入したこと | 必須 | [ ] | |
| 9.1.9 | `dnssec-policy`（KSK/ZSK自動ロールオーバー）・`inline-signing yes` をゾーン定義に設定したこと | 必須 | [ ] | |
| 9.1.10 | 廃止パラメーター（`dnssec-enable` / `dnssec-lookaside`）が設定されていないことを確認したこと | 必須 | [ ] | |
| 9.1.11 | SOA Serial（★YYYYMMDDnn形式）・A レコード（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 9.1.12 | TXT（SPF）レコードを設定したこと | 推奨 | [ ] | |
| 9.1.13 | TXT（DMARC）・CAA レコードを設定したこと | 推奨 | [ ] | |

### 9.2 DNSキャッシュサーバー（unbound 1.16）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 9.2.1 | `interface` の本番IPアドレス（★）を確定・記入したこと（0.0.0.0は非推奨） | 必須 | [ ] | |
| 9.2.2 | `access-control` で内部NW（★）を `allow`・`0.0.0.0/0 refuse` を設定したこと | 必須 | [ ] | |
| 9.2.3 | `hide-identity yes` / `hide-version yes` を設定したこと | 必須 | [ ] | |
| 9.2.4 | `harden-glue yes` / `harden-dnssec-stripped yes` を設定したこと | 必須 | [ ] | |
| 9.2.5 | `val-permissive-mode no`（DNSSEC検証厳格化）を設定したこと | 必須 | [ ] | |
| 9.2.6 | 内部ドメインのフォワード先（★BINDサーバーIP）を確定・記入したこと | 必須 | [ ] | |
| 9.2.7 | 外部ドメインのフォワード先上位DNS（★）を確定・記入したこと | 必須 | [ ] | |
| 9.2.8 | `use-caps-for-id yes`（0x20エンコーディング）を設定したこと | 推奨 | [ ] | |
| 9.2.9 | `prefetch yes` を設定したこと | 推奨 | [ ] | |
| 9.2.10 | `num-threads`（★CPUコア数）/ `msg-cache-size` / `rrset-cache-size`（★）を確定・記入したこと | 推奨 | [ ] | |

---

## 10. Webサーバー設定パラメーター

### 10.1 Apache（`/etc/httpd/conf/httpd.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 10.1.1 | `ServerName`（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 10.1.2 | `ServerTokens Prod`（バージョン情報最小表示）・`ServerSignature Off` を設定したこと | 必須 | [ ] | |
| 10.1.3 | `Options -Indexes`（ディレクトリリスティング禁止）を設定したこと | 必須 | [ ] | |

### 10.2 SSL/TLS設定（`/etc/httpd/conf.d/ssl.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 10.2.1 | `SSLProtocol` に TLSv1.2 / TLSv1.3 のみ許可したこと（SSLv3/TLS1.0/1.1は明示的に無効化） | 必須 | [ ] | |
| 10.2.2 | `SSLCertificateFile` / `SSLCertificateKeyFile`（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 10.2.3 | HSTS（`Strict-Transport-Security`）ヘッダーを設定したこと | 必須 | [ ] | |

### 10.3 Nginx（`/etc/nginx/nginx.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 10.3.1 | `ssl_protocols` に TLSv1.2 TLSv1.3 のみ許可したこと | 必須 | [ ] | |
| 10.3.2 | `server_tokens off` を設定したこと | 必須 | [ ] | |

---

## 11. ファイル共有設定パラメーター

### 11.1 Samba（`/etc/samba/smb.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 11.1.1 | `workgroup` / `realm`（★設定値）を確定・記入したこと | 推奨 | [ ] | |
| 11.1.2 | `min protocol = SMB2` を設定したこと（SMBv1無効化） | 推奨 | [ ] | |
| 11.1.3 | `ntlm auth = ntlmv2-only` を設定したこと | 推奨 | [ ] | |

### 11.2 NFS（`/etc/nfs.conf` + `/etc/exports`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 11.2.1 | NFSv4.2のみ有効化したこと（v2 / v3は無効） | 推奨 | [ ] | |
| 11.2.2 | `/etc/exports` のエクスポート先（★設定値）を確定・記入したこと | 推奨 | [ ] | |

---

## 12. メールサービス設定パラメーター

### 12.1 Postfix（`/etc/postfix/main.cf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 12.1.1 | `myhostname` / `mydomain`（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 12.1.2 | `smtpd_tls_security_level=encrypt`（STARTTLS必須）を設定したこと | 必須 | [ ] | |
| 12.1.3 | `smtpd_tls_cert_file` / `smtpd_tls_key_file`（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 12.1.4 | `mynetworks`（★設定値）を確定・記入したこと | 必須 | [ ] | |

### 12.2 Dovecot（`/etc/dovecot/dovecot.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 12.2.1 | `protocols` に imap / pop3 のみ（IMAPS/POP3S）有効化したこと | 必須 | [ ] | |
| 12.2.2 | `ssl_cert` / `ssl_key`（★設定値）を確定・記入したこと | 必須 | [ ] | |

---

## 13. DHCP / FTP / VPN 設定パラメーター

### 13.1 DHCP（`/etc/dhcp/dhcpd.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 13.1.1 | `subnet` / `netmask`・`range`（★設定値）を確定・記入したこと | 推奨 | [ ] | |
| 13.1.2 | `option routers` / `option domain-name-servers`（★設定値）を確定・記入したこと | 推奨 | [ ] | |
| 13.1.3 | `default-lease-time` / `max-lease-time` を設定したこと | 推奨 | [ ] | |

### 13.2 FTP（`/etc/vsftpd/vsftpd.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 13.2.1 | `anonymous_enable=NO` を設定したこと | 推奨 | [ ] | |
| 13.2.2 | `ssl_enable=YES`（FTPS必須）・`rsa_cert_file` / `rsa_private_key_file`（★設定値）を設定したこと | 推奨 | [ ] | |
| 13.2.3 | `pasv_min_port` / `pasv_max_port` を設定したこと | 推奨 | [ ] | |

### 13.3 OpenVPN（`/etc/openvpn/server/server.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 13.3.1 | `tls-crypt` を設定したこと（tls-authは旧方式） | 推奨 | [ ] | |
| 13.3.2 | `cipher` / `tls-cipher` に安全なアルゴリズムを設定したこと | 推奨 | [ ] | |
| 13.3.3 | `ca` / `cert` / `key`（★設定値）を確定・記入したこと | 推奨 | [ ] | |
| 13.3.4 | `server` のVPNサブネット（★設定値）を確定・記入したこと | 推奨 | [ ] | |

---

## 14. セキュリティ設定パラメーター

### 14.1 SELinux（`/etc/selinux/config`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 14.1.1 | `SELINUX=enforcing` を設定したこと | 必須 | [ ] | |
| 14.1.2 | `SELINUXTYPE=targeted` を設定したこと | 必須 | [ ] | |
| 14.1.3 | `setenforce 0`（一時変更）を本番で使用しないことを明記したこと | 必須 | [ ] | |

### 14.2 firewalld 許可サービス一覧

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 14.2.1 | SSHポート（★設定値）の許可を設定したこと | 必須 | [ ] | |
| 14.2.2 | DNS（TCP/UDP 53）の許可を設定したこと | 必須 | [ ] | |
| 14.2.3 | HTTP（80）/ HTTPS（443）の許可を設定したこと | 必須 | [ ] | |
| 14.2.4 | 不要なデフォルトサービスを削除したこと | 必須 | [ ] | |
| 14.2.5 | 許可ゾーン（internal / external / dmz等）を環境に応じて設定したこと | 必須 | [ ] | |

---

## 15. プロキシ設定パラメーター（`/etc/squid/squid.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 15.1 | `http_port`（★設定値）を確定・記入したこと | 推奨 | [ ] | |
| 15.2 | ACLで許可クライアントIPを限定したこと | 推奨 | [ ] | |
| 15.3 | `cache_mem` / `maximum_object_size` を設定したこと | 推奨 | [ ] | |

---

## 16. NTP設定パラメーター（`/etc/chrony.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 16.1 | `pool` / `server`（★設定値：プライマリ / セカンダリ）を確定・記入したこと | 必須 | [ ] | |
| 16.2 | `makestep` を設定したこと（起動時の大幅ズレ補正） | 必須 | [ ] | |
| 16.3 | `rtcsync` を設定したこと（RTC同期） | 推奨 | [ ] | |
| 16.4 | `allow`（★内部NW）を設定したこと（NTPサーバーとして使用する場合） | 推奨 | [ ] | |
| 16.5 | `leapsectz`（うるう秒対応）を設定したこと | 推奨 | [ ] | |

---

## 17. ファイル整合性監視パラメーター（`/etc/aide.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 17.1 | 監視対象ディレクトリ（/etc / /bin / /sbin / /usr等）を設定したこと | 推奨 | [ ] | |
| 17.2 | 監視除外ディレクトリ（/var/log等）を設定したこと | 推奨 | [ ] | |
| 17.3 | 初期DBの作成コマンド（`aide --init`）を手順として記載したこと | 推奨 | [ ] | |
| 17.4 | 定期チェックのスケジュール（systemd timer / cron）を設定したこと | 推奨 | [ ] | |

---

## 18. Fail2ban設定パラメーター（`/etc/fail2ban/jail.d/custom.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 18.1 | `[sshd]` jail を有効化したこと | 推奨 | [ ] | |
| 18.2 | `maxretry`（試行回数上限）を設定したこと | 推奨 | [ ] | |
| 18.3 | `bantime`（ブロック期間）を設定したこと | 推奨 | [ ] | |
| 18.4 | `findtime`（検出期間）を設定したこと | 推奨 | [ ] | |
| 18.5 | firewalldとの連携（`banaction=firewallcmd-ipset`）を設定したこと | 推奨 | [ ] | |

---

## 19. ディレクトリサービス設定パラメーター

### 19.1 FreeIPA（`ipa` コマンドで管理）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 19.1.1 | FreeIPAサーバーのドメイン・realm（★設定値）を確定・記入したこと | 推奨 | [ ] | |
| 19.1.2 | `ipa-server-install` の実行パラメーターを記載したこと | 推奨 | [ ] | |
| 19.1.3 | DNS統合方針（FreeIPA管理のDNS）を定義したこと | 推奨 | [ ] | |

### 19.2 SSSD（`/etc/sssd/sssd.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 19.2.1 | `domains`（★設定値）・`krb5_realm` / `ldap_search_base`（★設定値）を確定・記入したこと | 推奨 | [ ] | |
| 19.2.2 | `cache_credentials=True` を設定したこと | 推奨 | [ ] | |
| 19.2.3 | SSSDサービスの自動起動を設定したこと | 推奨 | [ ] | |

---

## 20. ログ・ジョブスケジューラー設定パラメーター

### 20.1 rsyslog（`/etc/rsyslog.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 20.1.1 | リモートSyslogサーバーへの転送設定（★サーバーIP / ポート）を確定・記入したこと | 推奨 | [ ] | |
| 20.1.2 | TLS転送（`omfwd` + `StreamDriverMode=1`）を設定したこと | 推奨 | [ ] | |

### 20.2 logrotate（`/etc/logrotate.d/`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 20.2.1 | 各ログの `rotate` 世代数を設定したこと | 必須 | [ ] | |
| 20.2.2 | `compress` / `delaycompress` / `missingok` / `notifempty` を設定したこと | 推奨 | [ ] | |

### 20.3 ジョブスケジューラー設定

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 20.3.1 | 定期ジョブにsystemd timerを優先使用する方針を確定したこと | 推奨 | [ ] | |
| 20.3.2 | cronに残す用途（★）を限定・記入したこと | 推奨 | [ ] | |

---

## 21. 監査（auditd）設定パラメーター

### 21.1 auditd設定（`/etc/audit/auditd.conf`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 21.1.1 | `log_file`（`/var/log/audit/audit.log`）・`max_log_file` を設定したこと | 必須 | [ ] | |
| 21.1.2 | `max_log_file_action=ROTATE` を設定したこと | 必須 | [ ] | |
| 21.1.3 | `num_logs`（保管世代数）を180日以上の保管を満たすよう設定したこと | 必須 | [ ] | |
| 21.1.4 | `space_left_action=SYSLOG` を設定したこと | 推奨 | [ ] | |

### 21.2 監査ルール（`/etc/audit/rules.d/99-custom.rules`）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 21.2.1 | `/etc/passwd` / `/etc/shadow` の書き込み・属性変更監視を設定したこと | 必須 | [ ] | |
| 21.2.2 | `/etc/sudoers` / `/etc/sudoers.d/` の書き込み監視を設定したこと | 必須 | [ ] | |
| 21.2.3 | root実行コマンド（execve / euid=0）の記録を設定したこと | 必須 | [ ] | |
| 21.2.4 | `-e 2`（監査ルールの変更不可）を末尾に設定したこと | 推奨 | [ ] | |

---

## 22. NTP冗長化設定パラメーター

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 22.1 | プライマリNTPサーバー（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 22.2 | セカンダリNTPサーバー（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 22.3 | `chronyc tracking` による同期確認コマンドを手順として記載したこと | 必須 | [ ] | |

---

## 23. バックアップ設定パラメーター

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 23.1 | バックアップ対象ディレクトリ（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 23.2 | バックアップ先パス（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 23.3 | バックアップスケジュール（systemd timer / cron）を設定したこと | 必須 | [ ] | |
| 23.4 | GFS世代管理（日次7 / 週次4 / 月次12）を設定したこと | 必須 | [ ] | |
| 23.5 | リストア試験の手順を記載したこと | 推奨 | [ ] | |

---

## 24. ログ転送設定パラメーター

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 24.1 | 転送先Syslogサーバー（★IPアドレス / ポート）を確定・記入したこと | 推奨 | [ ] | |
| 24.2 | TLS証明書（★設定値）を確定・記入したこと | 推奨 | [ ] | |
| 24.3 | SIEM連携のフォーマット（RFC5424等）を定義したこと | 推奨 | [ ] | |

---

## 25. EDR・マルウェア対策パラメーター

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 25.1 | 導入するEDR製品名（★）を確定・記入したこと | 推奨 | [ ] | |
| 25.2 | ClamAV等のマルウェア対策設定を定義したこと | 推奨 | [ ] | |
| 25.3 | 定期スキャンのスケジュールを設定したこと | 推奨 | [ ] | |

---

## 26. 監視設定パラメーター

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 26.1 | 監視対象ホスト（★設定値）を確定・記入したこと | 必須 | [ ] | |
| 26.2 | CPU / Memory / Disk 使用率の閾値（Warning/Critical）を設定したこと | 必須 | [ ] | |
| 26.3 | inode 使用率の閾値を設定したこと | 必須 | [ ] | |
| 26.4 | 死活監視対象プロセス（httpd / named / unbound / sshd等）を設定したこと | 必須 | [ ] | |
| 26.5 | TCPポート監視対象（22 / 53 / 80 / 443等）を設定したこと | 必須 | [ ] | |
| 26.6 | SSL/TLS証明書の期限監視（★日数閾値）を設定したこと | 推奨 | [ ] | |
| 26.7 | NTP同期監視（`chronyc tracking` 確認）を設定したこと | 推奨 | [ ] | |

---

## 27. パッチ管理設定パラメーター

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 27.1 | `dnf-automatic` の設定（`download_updates` / `apply_updates`）を定義したこと | 必須 | [ ] | |
| 27.2 | セキュリティパッチのみ適用する設定（`upgrade_type=security`）を設定したこと | 必須 | [ ] | |
| 27.3 | パッチ適用前のスナップショット取得手順を記載したこと | 推奨 | [ ] | |
| 27.4 | 月次パッチ適用スケジュール（systemd timer）を設定したこと | 必須 | [ ] | |

---

## 28. sudo設定パラメーター

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 28.1 | `/etc/sudoers.d/` 配下にロール別ファイルを分割作成したこと | 必須 | [ ] | |
| 28.2 | ops（運用管理者）ロールの許可コマンドを定義したこと | 必須 | [ ] | |
| 28.3 | dba（DB管理者）ロールの許可コマンドを定義したこと | 必須 | [ ] | |
| 28.4 | app（AP管理者）ロールの許可コマンドを定義したこと | 必須 | [ ] | |
| 28.5 | sec（セキュリティ担当）ロールの許可コマンドを定義したこと | 必須 | [ ] | |
| 28.6 | `NOPASSWD` の使用箇所を最小限に限定したこと | 必須 | [ ] | |
| 28.7 | `Defaults logfile` 等によるsudoログを設定したこと | 推奨 | [ ] | |

---

## 29. サービス自動復旧設定パラメーター

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 29.1 | httpd の Drop-inファイル（`Restart=always` / `RestartSec=10`）を設定したこと | 推奨 | [ ] | |
| 29.2 | nginx の Drop-inファイル（`Restart=always` / `RestartSec=10`）を設定したこと | 推奨 | [ ] | |
| 29.3 | postfix の Drop-inファイル（`Restart=always` / `RestartSec=10`）を設定したこと | 推奨 | [ ] | |
| 29.4 | named（権威DNS）の Drop-inファイル（`Restart=always` / `RestartSec=10`）を設定したこと | 推奨 | [ ] | |
| 29.5 | unbound（キャッシュDNS）の Drop-inファイル（`Restart=always` / `RestartSec=10`）を設定したこと | 推奨 | [ ] | |
| 29.6 | sshd の Drop-inファイル（`Restart=always` / `RestartSec=5`）を設定したこと | 推奨 | [ ] | |
| 29.7 | chronyd の Drop-inファイル（`Restart=always` / `RestartSec=30`）を設定したこと | 推奨 | [ ] | |
| 29.8 | `systemctl daemon-reload` 後に各サービスのRestart設定が反映されていることを確認したこと | 推奨 | [ ] | |

---

## 30. 運用管理サービス設定パラメーター

### 30.1 SSH公開鍵集中管理（FreeIPA連携）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 30.1.1 | `AuthorizedKeysCommand /usr/bin/sss_ssh_authorizedkeys %u` を設定したこと | 推奨 | [ ] | |
| 30.1.2 | `AuthorizedKeysCommandUser nobody` を設定したこと | 推奨 | [ ] | |

### 30.2 Ansible構成管理パラメーター

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| 30.2.1 | `ansible.cfg` の `remote_user`（★管理ユーザー名）を確定・記入したこと | 推奨 | [ ] | |
| 30.2.2 | `ansible.cfg` の `private_key_file`（★Ed25519鍵パス）を確定・記入したこと | 推奨 | [ ] | |
| 30.2.3 | `host_key_checking=True`（本番環境では必須）を設定したこと | 推奨 | [ ] | |
| 30.2.4 | Ansibleプレイブックの構文チェック（`--syntax-check`）手順を記載したこと | 推奨 | [ ] | |

---

## 全体整合性チェック

### パラメーター確定確認（★設定値残存チェック）

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| Z.1 | ホスト名・IPアドレス・ゲートウェイ・DNSサーバーの★設定値を全て記入したこと | 必須 | [ ] | |
| Z.2 | SSHポート番号・AllowUsers / AllowGroupsの★設定値を確定したこと | 必須 | [ ] | |
| Z.3 | named.conf の listen-on / allow-query / allow-transferの★設定値を確定したこと | 必須 | [ ] | |
| Z.4 | unbound.conf の interface / access-control / forward先の★設定値を確定したこと | 必須 | [ ] | |
| Z.5 | SOA Serial / Aレコード等ゾーンファイルの★設定値を確定したこと | 必須 | [ ] | |
| Z.6 | 証明書パス（Web / Mail / FTP / OpenVPN）の★設定値を確定したこと | 必須 | [ ] | |
| Z.7 | NTPサーバー・Syslogサーバー・SIEM転送先の★設定値を確定したこと | 必須 | [ ] | |
| Z.8 | FreeIPA ドメイン / realm / ldap_search_base の★設定値を確定したこと | 必須 | [ ] | |

### 前章との整合性確認

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| Z.9 | 通信表（2.1）とfirewalld許可ルール（14.2）に過不足がないか確認したこと | 必須 | [ ] | |
| Z.10 | 基本設計書のサービス選定と詳細設計のパラメーター設定が矛盾していないか確認したこと | 必須 | [ ] | |
| Z.11 | 本書の数値（ログ保持期間・パスワードポリシー等）が要件定義書の要件と矛盾していないか確認したこと | 必須 | [ ] | |

### レビュー・承認

| # | チェック項目 | 優先度 | 確認 | 備考 |
|---|------------|--------|------|------|
| Z.12 | インフラ担当者によるレビューを受けたこと | 必須 | [ ] | |
| Z.13 | セキュリティ担当によるレビューを受けたこと（8〜14章） | 必須 | [ ] | |
| Z.14 | 運用担当によるレビューを受けたこと（20〜29章） | 必須 | [ ] | |

---

## 🔷 サマリー（確認完了集計）

| カテゴリ | 必須項目数 | 確認済み | 推奨項目数 | 確認済み |
|---------|-----------|---------|-----------|---------| 
| 1. 目的・スコープ | 5 | /5 | 4 | /4 |
| 2. システム概要 | 6 | /6 | 1 | /1 |
| 3. OS基本設定 | 10 | /10 | 1 | /1 |
| 4. カーネルパラメーター | 11 | /11 | 1 | /1 |
| 5. ストレージ | 5 | /5 | 0 | /0 |
| 6. ネットワーク | 7 | /7 | 0 | /0 |
| 7. SSH | 11 | /11 | 1 | /1 |
| 8. ユーザー・PAM | 9 | /9 | 0 | /0 |
| 9.1 DNS（BIND） | 11 | /11 | 2 | /2 |
| 9.2 DNS（unbound） | 7 | /7 | 3 | /3 |
| 10. Webサーバー | 8 | /8 | 0 | /0 |
| 11. ファイル共有 | 0 | /0 | 5 | /5 |
| 12. メールサービス | 6 | /6 | 0 | /0 |
| 13. DHCP/FTP/VPN | 0 | /0 | 10 | /10 |
| 14. SELinux / firewalld | 8 | /8 | 0 | /0 |
| 15. プロキシ | 0 | /0 | 3 | /3 |
| 16. NTP | 2 | /2 | 3 | /3 |
| 17. AIDE | 0 | /0 | 4 | /4 |
| 18. Fail2ban | 0 | /0 | 5 | /5 |
| 19. ディレクトリサービス | 0 | /0 | 6 | /6 |
| 20. ログ・スケジューラー | 1 | /1 | 5 | /5 |
| 21. 監査（auditd） | 6 | /6 | 1 | /1 |
| 22. NTP冗長化 | 3 | /3 | 0 | /0 |
| 23. バックアップ | 4 | /4 | 1 | /1 |
| 24. ログ転送 | 0 | /0 | 3 | /3 |
| 25. EDR | 0 | /0 | 3 | /3 |
| 26. 監視 | 5 | /5 | 2 | /2 |
| 27. パッチ管理 | 3 | /3 | 1 | /1 |
| 28. sudo | 6 | /6 | 1 | /1 |
| 29. 自動復旧 | 0 | /0 | 8 | /8 |
| 30. 運用管理 | 0 | /0 | 6 | /6 |
| ★設定値残存・整合性・承認 | 14 | /14 | 0 | /0 |
| **合計** | **148** | **/148** | **80** | **/80** |

---

## 備考・メモ欄

| 項目 | 内容 |
|------|------|
| プロジェクト名 | |
| 担当者 | |
| 作成日 | |
| レビュー日 | |
| 環境 | 開発 / 検証 / 本番 |
| 関連文書 | Linux サーバー構築 基本設計書 v1.0 / 基本設計チェックリスト v1.0 |

---

*本チェックリストの全項目クリア後、Ansibleコード実装フェーズへ移行してください。*  
*要件定義 → 基本設計 → 詳細設計の各チェックリストは、同じ非機能要件項目（可用性/性能/信頼性/セキュリティ/運用/コスト）を一貫した観点で追跡できるよう設計されています。*
